## 📅 2026-04-21
**PRG 42576 - 완주하지 못한 선수**
**PRG 01845 - 폰켓몬**
**PRG 42578 - 의상**

### PRG 42576 - 완주하지 못한 선수
- **Topic:** Hash Map ( std::unordered_map )
- **Folder:** `Week_03/01_Hash/`
- **Time Complexity:** O(N)
- **Space Complexity:** O(N)

- **Key Idea:**
두 배열의 차집합을 구하기 위해, 해시 맵에 이름을 키( Key )로, 등장 횟수를 값( Value )으로 저장하여 카운팅

- **Caution:**
std::string을 루프에서 값 복사( Pass by Value )로 넘기면 심각한 메모리 오버헤드 발생.

- **Improvement:**
map.find( ) 대신 map[key]++의 기본 생성( Default Initialization ) 특성을 활용하여 중복 탐색 비용 제거.

- **Problem Hint:**
"단 한 명의 선수를 제외하고는 모든 선수가 마라톤을 완주" -> 1:1 매칭 후 남는 단일 데이터 색출.

- **Portfolio Selling Point:**
C++에서 무거운 객체( String )을 다룰 때 const string&를 사용하여 불필요한 깊은 복사( Deep Copy )를 원천 차단하는 메모리 방어적 코딩 습관 증명.

---

### PRG 01845 - 폰켓몬
- **Topic:** Hash Set ( std::unordered_set )
- **Folder:** `Week_03/01_Hash/`
- **Time Complexity:** O(N)
- **Space Complexity:** O(N)

- **Key Idea:**
선택 가능한 최대 마리 수( N/2 )와 해시 셋을 통해 중복을 제거한 '고유한 종류 수'를 구한 뒤, 둘 중 더 작은 값을 도출.

- **Caution:**
STL 컨테이너의 size( )는 size_t ( unsigned )를 반환하므로, std::min 함수 사용 시 int와의 타입 충돌로 인한 컴파일 경고/에러 주의.

- **Improvement:**
별도의 if-else 분기 없이 min 함수 한 줄로 논리 구조 압축

- **Problem Hint:**
"가장 많은 종류의 폰켓몬을 선택" -> 값 자체보다 고유한 '종류'의 개수( 중복 제거 )가 핵심.

- **Portfolio Selling Point:**
데이터의 존재 유무와 고유성만 필요할 때 map 대신 set을 채택하여 메모리 풋프린트를 최소화하고, ( int ) 명시적 캐스팅을 통해 실무 환경에서의 엄격한 타입 불일치 에러를 방어하는 디테일.

---

### PRG 42578 - 의상
- **Topic:** Hash Map | Combinations
- **Folder:** `Week_03/01_Hash/`
- **Time Complexity:** O(N)
- **Space Complexity:** O(K) ( K = 옷의 카테고리 종류 수 )

- **Key Idea:**
각 카테고리별 아이템 개수에 '착용하지 않음( +1 )' 상태를 더해 모두 곱한 뒤, 전체를 미착용한 경우( -1 )를 제외하는 조합 연산.

- **Caution:**
누적 곱셈 시 경우의 수가 기하급수적으로 커져 int 범위를 초과할 수 있는 오버플로우 위험.

- **Improvement:**
long long 타입으로 선언하여 큰 수 연산 대비.

- **Problem Hint:**
"서로 다른 옷의 조합의 수", "하루에 최소 한 개의 의상은 입습니다" -> 카테고리별 독립 시행 및 공집함 제외.

- **Portfolio Selling Point:**
C++17 구조적 바인딩( [ key, val ] )을 적극 도입하여 가독성을 높이고, 게임 내 캐릭터 코스튬/장비 슬롯 시스템 등에서 발생할 수 있는 복잡한 경우으 ㅣ수를 최적화된 수학적 모델로 구현할 수 있는 아키텍처적 사고 증명.

---

## 📅 2026-04-22
**PRG 42586 - 기능개발**
**PRG 42587 - 프로세스**

### PRG 42586 - 기능개발
- **Topic:** Queue ( FiFO 스케줄링 )
- **Folder:** `Week_03/02_Stack_Queue/`
- **Time Complexity:** O(N)
- **Space Complexity:** O(N)

- **Key Idea:**
뒤에 있는 기능이 먼저 개발되어도 앞의 기능이 배포될 때 함께 배포되어야 하므로, FIFO 자료구조인 Queue를 사용하여 기준일( day )보다 작거나 같은 후속 작업들을 일괄적으로 pop 하며 카운팅.

- **Caution:**
남은 일수 계산 시 몫과 나머지를 정확히 처리하지 않으면 배포일이 하루씩 밀리거나 당겨지는 논리적 오류 발생.

- **Improvement:**
<cmath>의 ceil( ) 함수나 double 형병환을 피하고 순수 정수 연산으로 올림을 구현하여 퍼포먼스 향상.

- **Problem Hint:**
"앞에 있는 기능이 먼저 배포되어야 합니다." -> Fisrt-In-First-Out ( FIFO ) 구조 도출.

- **Portfolio Selling Point:**
부동소수점 연산 오버헤드와 정밀도 오류를 피하기 위해, 모듈로( % ) 연산자를 활용한 순수 정수 수학( Integer Math ) 기반의 최적화된 올림 로직을 구현하여 엔진 프로그래머로서의 성능 튜닝 감각을 증명.

---

### PRG 42587 - 프로세스
- **Topic:** Queue | Priority Queue ( Max Heap )
- **Folder:** `Week_03/02_Stack_Queue/`
- **Time Complexity:** O(N log N) ( Priority Queue 삽입/삭제 기준 )
- **Space Complexity:** O(N) ( 두 개의 큐 메모리 할당 )

- **Key Idea:**
`queue<pair<우선순위, 인덱스>>`로 프로세스의 본래 위치( location )를 추적하고, priority_queue로 현재 큐에 남은 최댓값을 즉시 파악하여 프로세스 순환( Circular ) 구조를 제어.

- **Caution:**
큐에 우선순위 값만 넣으면, 나중에 꺼냈을 때 해당 프로세스가 내가 찾던 타겟( location )인지 식별할 수 없으므로 반드시 고유 인덱스를 구조체나 pair로 묶어서 관리해야 함.

- **Improvement:**
큐 요소 삽입 시 `push({a, b})` 방식 대신 가변 인자 템플릿인 `emplace(a, b)`를 사용하여 불필요한 복사 생성자 호출 방지.

- **Problem Hint:** 지문 키워드 → 접근법
"더 높은 우선순위가 있다며 다시 큐의 뒤에 넣습니다" -> 데이터를 뽑아서 뒤로 다시 넣는 순환 큐( Circular Queue ) 및 최댓값 지속 추적( Max Heap ) 도출.

- **Portfolio Selling Point:**
게임 엔진의 이벤트 디스패처( Event Dispatcher )가 긴급한 작업( Network, Render )과 일반 작업( UI, Sound )을 스케줄링하는 로직과 동일한 아키텍처. 두 개의 독립적인 STL 컨테이너( queue, priority_queue )를 결합하여 선형 탐색의 비효율성을 극복하는 시스템 설계 능력을 입증함.

---

## 📅 2026-04-23
**PRG 01844 - 게임 맵  최단거리**
**PRG 43162 - 네트워크**

### PRG 01844 - 게임 맵 최단거리
- **Topic:** Graph | BFS 
- **Folder:** `Week_03/03_Graph/`
- **Time Complexity:** O(N * M) *( 맵의 모든 칸을 최대 1번씩 방문 )*
- **Space Complexity:** O(N * M) *(visited 배열 및 Queue 메모리 )*

- **Key Idea:**
시작점에서부터의 최단 거리를 보장하기 위해 너비 우선 탐색( BFS )를 사용.
큐에 좌표를 넣으며 거리를 1씩 누적.

- **Caution:**
목적지에 도달하지 못하는 막힌 지형의 경우 예외 처리( -1 반환 )가 필요함.

- **Improvement:**
방문 여부 배열(`visited`)과 거리 배열(`dist`)을 분리하지 않고, `memset`을 통해 `-1`로 초기화하여 두 가지 목적을 하나의 배열로 통합 처리.

- **Problem Hint:** 
"상대 팀 진영에 최대한 빨리 도착하는 것이 유리", "최단거리" -> 깊게 파고드는 DFS가 아닌, 동심원처럼 퍼져나가며 무조건 최단 거리를 보장하는 BFS 알고리즘 도출.

- **Portfolio Selling Point:**
C++ 정적 분석기(MSVC Code Analysis )가 탐지할 수 있는 빈 배열 메모리 참조 에러(C6385, OOB )의 원리를 이해하고, 방어적 프로그래밍( Guard Clause )을 통해 안전하게 메모리를 다루는 실무적 시각을 입증.

---

### PRG 43162 - 네트워크
- **Topic:** Graph | Iterative DFS 
- **Folder:** `Week_03/03_Graph/`
- **Time Complexity:** O(N^2) ( 인접 행렬( Adjacency Matrix )의 모든 연결 상태를 확인 )
- **Space Complexity:** O(N) ( visited 배열 및 DFS 탐색용 std::stack 메모리 )

- **Key Idea:**
전체 노드( 컴퓨터 )를 순회하며 아직 방문하지 않은 노드가 발견될 때마다 새로운 네트워크 그룹으로 간주( 정답 + 1)하고, 해당 노드와 연결된 모든 노드를 스택 기반의 깊이 우선 탐색( DFS )으로 끝까지 파고들며 방문 처리함. ( 연결 요소 개수 찾기 )

- **Caution:**
스택을 활용한 반복문 DFS 구현 시, 바깥쪽 루프의 인덱스( i )와 큐/스택에서 꺼낸 현재 노드의 인덱스( cur )를 혼동하여 참조하면 무한 루프나 잘못된 탐색이 발생할 수 있으므로 변수 스코프 관리에 극도로 주의해야 함.

- **Improvement:**
구현이 쉬운 재귀( Recursive ) DFS를 사용하지 않고, 명시적인 std::stack 자료구조를 활용하여 OS 레벨의 스택 메모리 한계를 우회하는 안전한 루프 기반 로직으로 설계함.

- **Problem Hint:**
"A와 B가 연결되어 있고, B와 C가 연결되어 있으면 A와 C도 연결된 것.", " 네트워크 의 개수" -> 전형적인 '연결 요소( Connected Component )'의 개수를 구하는 그래프 탐색( DFS/BFS ) 문제임을 도출.

- **Portfolio Selling Point:**
대규모 데이터나 깊은 트리/그래프 구조를 다룰 때 발생할 수 있는 치명적인 콜 스택 오버플로우( Call Stack Overflow ) 크래시의 위험성을 인지하고, 이를 방지하기 위해 힙 메모리를 사용하는 std::stack 기반의 Iterative DFS를 직접 설계 및 디버깅해 낸 엔진 프로그래머로서의 메모리 안전성( Memory Safety ) 감각을 입증.

---

## 📅 2026-04-24
**PRG 43105 - 정수 삼각형**
**PRG 42895 - N으로 표현**

### PRG 43105 - 정수 삼각형
- **Topic:** DP | Bottom-Up | 1D Memoization
- **Folder:** `Week_03/04_DP/`
- **Time Complexity:** O(N^2) ( 삼각형의 모든 노드를 한 번씩 방문 )
- **Space Complexity:** O( 1차원 DP 배열 메모리 )

- **Key Idea:**
위에서 아래로 내려가는 대신, 바닥에서부터 꼭대기로 올라가는 바텀업( Bottom-Up ) 방식을 채택.
현재 층의 노드가 가질 수 있는 최댓값은 바로 아래층의 연결된 두 자식 노드 중 더 큰 값을 현재 노드의 값에 더하는 방식으로 누적함.

- **Caution:**
위에서 아래로( Top-Down ) 탐색할 경우, 삼각형의 가장 왼쪽 변과 오른쪽 변에서 배열 인덱스를 벗어나는 오류( Out of Bounds )가 발생하므로 이를 위한 예외 처리 분기문이 추가로 필요해짐.

- **Improvement:**
N 크기의 2차원 DP 배열( O( N^2 ))을 생성하지 않고, 크기 N의 차원 DP 배열( O( N )) 하나만 생성하여 이전 층의 결과를 덮어쓰는( In-place ) 방식으로 공간 복잡도를 획기적으로 낮춤.

- **Problem Hint:**
"삼각형의 꼭대기에서 바닥까지 이어지는 경로.", "거쳐간 숫자의 합이 가장 큰 경우" -> 경로의 부분합이 전체의 최적해를 구성하는 최적 부분 구조( Optimal Substructure )를 가짐을 파악하여 DP 알고리즘 도출.

- **Portfolio Selling Point:**
단순히 알고리즘을 푸는 것에 그치지 않고, 상태 전이의 규칙을 분석하여 2차원 배열을 1차원 배열로 압축하는 고도의 메모리 공간 최적화( Space Optimization ) 능력을 보여줌. 동시에 원본 인자( triangle )의 불변성( Immutability )을 유지하여 실무적인 API 설계 원칙을 준수하는 시각을 어필.

---

### PRG 42895 - N으로 표현
- **Topic:** DP | Set | Combinatorics
- **Folder:** `Week_03/04_DP/`
- **Time Complexity:** O(C) ( C는 N=8일 때 생성되는 모든 조합의 수, 상수에 가까움 )
- **Space Complexity:** O(K) ( K는 생성된 고유한 연산 결과값의 개수. unordered_set 메모리 )

- **Key Idea:**
숫자 N을 k번 사용해서 만들 수 있는 수의 집합을 DP[k]라고 할 때, DP[k]는 ( DP[i]의 원소 )와 ( DP[k-i]의 원소 )를 사칙연산한 결과들의 합집합임을 이용하여 상태 전이( State Transition )를 구현함. 추가로 N이 연속되는 수( N, NN, NNN... )를 미리 집합에 초기화함.

- **Caution:**
다중 루프가 5중으로 겹치기 때문에 인덱스 설계( 0-based 인덱스의 j와 i-j-1 분할 )가 하나라도 어긋나면 올바른 조합이 형성되지 않음. 또한, 메인 DP 연산뿐만 아니라 '연속된 숫지'를 배열에 초기화하는 단계에서도 목표 숫자( number )와 일치하는지 반드시 검사해야 함. 이를 누락하면 목표 숫자가 초기화 값( 예 : 55 )과 같을 때 정답을 무시하고 넘어가 오답( -1 )을 반환하는 논리적 오류가 발생함.

- **Improvement:**
가독성을 해치고 제어문 이탈 범위가 모호한 switch-case-break 구조를 if - else if - continue로 리팩토링하여 예기치 않은 쓰레기값 삽입 버그를 원천 차단함. 배열 초기화 구역에 단 한줄의 조기 탈출( Early Exit : if(nn = number ) return i + 1;) 코드를 추가하여 엣지 케이스 방어와 연산 속도 최적화를 동시에 달성함.

- **Problem Hint:**
"최솟값이 8보다 크면 -1을 return" -> 상태 공간( State Space )의 깊이가 8로 극단적으로 제한되어 있음을 파악하고, 깊이가 얕을 때는 다중 루프를 통한 모든 경우의 수 탐색( Exhaustive Search )과 DP의 결합이 최적의 해답임을 도출.

- **Portfolio Selling Point:**
단순히 알고리즘이 돌아가게 만드는 것을 넘어, 제어 흐름( Control Flow )을 실무 친화적으로 리팩토링하는 클린 코드 역량을 보여줌. 특히, 메인 루프에 진입하기 전 시스템의 '초기 상태( Initial State )'에 대한 검증 로직을 잊지 않고 구현하여, 놓치기 쉬운 엣지 케이스까지 완벽하게 잡아내는 꼼꼼한 QA( Quality Assurance ) 마인드를 입증함.

---

## 📅 2026-04-27
**PRG 12978 - 배달**
**PRG 72413 - 합승 택시 요금**

### PRG 12978 - 배달
- **Topic:** Graph | Dijkstra | Priority Queue
- **Folder:** `Week_04/01_Graph_Advanced/`
- **Time Complexity:** O(E log V) ( E는 간선의 개수, V는 마을의 개수. 힙 연산 기준 )
- **Space Complexity:** O(V + E) ( 인접 리스트 배열 및 우선순위 큐 메모리 )

- **Key Idea:**
마을 간의 이동 비용( 시간 )이 다르므로 일반적인 BFS가 아닌 다익스트라( Dijkstra ) 알고리즘을 사용. 최단 거리 테이블( visited )을 무한대( INF )로 초기화한 뒤, 1번 마을을 시작으로 우선순위 큐( Min-Heap )를 이용해 현재 가장 비용이 적게 드는 마을부터 순차적으로 탐색하며 연결된 마을들의 최단 거리를 갱신함.

- **Caution:**
두 마을을 연결하는 도로가 여러 개일 수 있다는 조건( 다중 간선 )이 있ㄴ으나, 다익스트라의 특성상 큐에 여러 간선 정보가 들어가더라도 최솟값을 먼저 꺼내어 테이블을 갱신( visited[nnode] = ncost )하고, 이후에 꺼내지는 더 큰 비용의 간선은 cost > visited[node] 조건에 의해 자동으로 필터링되므로 로직상 안전함.

- **Improvement:**
전역 공간( Data Segment )에 할당된 2차원 벡터 배열( adj )은 로컬 환경에서 함수가 연속 호출될 때 이전 데이터가 누적되는 부작용을 일으킴. 코딩 테스트상에선 안전하지만 테스트가 불편하다면 adj[i].clear( )를 명시적으로 호출하여 시스템의 상태를 안전하게 초기화하는 방어 코드를 추가할 수 있음.

- **Problem Hint:** 
"음식 배달이 가능한 마을의 개수", "도로를 지날 때 걸리는 시간" -> 간선에 양의 가중치가 존재하는 그래프에서 단일 출발점 최단 경로( Single-Source Shortest Path )를 찾는 전형적인 다익스트라 모델 도출.

- **Portfolio Selling Point:**
네비게이션 메시( NavMesh )나 AI 길찾기의 근간이 되는 다익스트라 알고리즘을 C++ prioirty_queue를 이용해 최적화된 형태로 구현함. 특히 로컬 테스트 중 발생한 메모리 잔존 문제( Data 영역 찌꺼기 )를 스스로 진단하고 해결함으로써, 메모리의 생명 주기와 엔진 런타임 환경에 대한 깊은 이해도를 증명.

---

### PRG 72413 - 합승 택시 요금
- **Topic:** Graph | Dijkstra | Multi-Source
- **Folder:** `Week_04/01_Graph_Advanved/`
- **Time Complexity:** O(E log V) ( 다익스트라 3회 호출. E는 간선, V는 노드 수 )
- **Space Complexity:** O(V + E)

- **Key Idea:**
특정 지점 i까지 합승한 뒤 흩어지는 것이 최소 비용이라고 가정할 때, 총비용은 `( S -> i ) + ( i -> A ) + ( i -> B )`가 됨. 무방향 그래프의 특성에 따라 `( i -> A )`는 `( A -> i)`와 같으므로, 출발점 S, 도착점 A, 도착점 B를 각각 시작점으로 삼아 다익스트라를 총 3번만 수행하면 모든 임의의 분기점 i에 대한 비용을 한 번의 O(N) 반복문으로 도출할 수 있음.

- **Caution:**
다익스트라 알고리즘 결과를 3개 합산( `FromS[i] + FromA[i] + FromB[i]` )할 때, 도달 불가능한 경로의 초기값( `INF` )이 너무 크면 C++의 int 자료형 상한선( 약 21억 )을 초과하여 음수로 오버플로우되는 치명적인 버그가 발생할 수 있으므로 안전한 한계산( `1e8` 등 )을 설정해야 함.

- **Improvement:**
다익스트라 알고리즘을 메인 함수 내부에 중복 작성하지 않고 별도의 모듈화된 함수( `vector<int> dijkstra(...)`)로 분리하여 유지보수성을 높이고 코드를 간결하게 리팩토링함.

- **Problem Hint:**
"합승을 하거나 하지 않고", "최저 예상 택시요금을 계산" -> 경로가 어디선가 갈라진다는 '분기점'의 개념을 도출하고, 모든 노드를 분기점 후보로 두고 탐색해야 함을 파악.

- **Portfolio Selling Point:**
알고리즘의 맹목적인 구현에 그치지 않고, 무방향 그래프의 성질을 수학적으로 분석하여 연산 횟수를 획기적으로 줄이는 최적화 사고력을 입증함. 또한 시스템 내부 데이터 타입( `int` )의 한계를 인지하고 오버플로우 방지 설계를 선제적으로 적용하는 등, 실무 지향적인 메모리 안전성 감각을 뽐냄.

---

## 📅 2026-04-28
**PRG 42861 - 섬 연결하기**

### PRG 42861 - 섬 연결하기
- **Topic:** Graph | MST | Kruskal | Union-Find
- **Folder:** `Week_04/02_MST/`
- **Time Complexity:** O(E log E) ( 간선( E )을 가중치 기준으로 정렬하는 데 걸리는 시간이 지배적 )
- **Space Complexity:** O(V) ( 노드( V )의 개수만큼 필요한 parent 배열 메모리 )

- **Key Idea:**
모든 섬을 최소 비용으로 연결하기 위해 크루스칼 알고리즘을 사용함. 간선 배열을 비용 기준으로 오름차순 정렬한 뒤, 비용이 적은 간선부터 순서대로 선택. 이때 두 섬이 이미 같은 그룹에 속해 있는지( 사이클 발생 여부 )를 유니온-파인드 자료구조로 검사하여, 다른 그룹일 때만 연결하고 합산함.

- **Caution:**
두 가지 치명적인 엣지 케이스 및 문법 함정에 주의해야 함. 첫째, unionParent 함수 내부에서 두 노드의 최상위 부모가 같을 때( 사이클 발생 시 ) 즉시 return false;로 함수를 종료해야 하며, 이를 누락할 경우 로직이 무너짐. 둘째, std::sort의 커스텀 정렬 함수로 클래스 내부의 일반 멤버 함수를 넘기면 인자 개수 불일치( this 포인터 포함 )로 컴파일 에러가 발생함.

- **Improvement:**
std::sort의 타입 충돌 문제를 피하기 위해 함수를 static으로 선언하거나, 더 직관적이고 캡슐화가 잘 되는 모던 C++의 람다식을 활용하여 인라인으로 정렬 로직을 깔끔하게 처리함.

- **Problem Hint:**
"모든 섬이 서로 통행 가능", "최소 비용" -> 전형적인 최소 신장 트리( MST ) 문제의 핵심 키워드임을 도출.

- **Portfolio Selling Point:**
알고리즘의 논리적 흐름( Kruskal )뿐만 아니라, C++ 객체지향 프로그래밍에서 클래스 인스턴스와 멤버 함수의 메모리 구조( this 포인터 동작 원리 )를 정확히 이해하고 트러블슈팅하는 능력을 보여줌. 제어문( return )의 미세한 누락이 전체 아키텍처에 미치는 영향을 파악하는 꼼꼼한 디버깅 감각.

---

## 📅 2026-04-29
**PRG 87377 - 교점에 별 만들기**

### PRG 87377 - 교점에 별 만들기
- **Topic:** Math | Geometry | Graphics | Viewport Transform
- **Folder:** `Week_04/03_GameMath/`
- **Time Complexity:** O(N^2) ( N은 직선의 개수, 최대 1,000개의 직선에 대해 모든 쌍을 검사하므로 약 500,000번 연산 )
- **Space Complexity:** O(W * H) ( W와 H는 정수 교점들을 포함하는 Bounding Box의 가로, 세로 길이로, 결과 문자열 배열이 차지하는 메모리 )

- **Key Idea:**
O(N^2)의 완전 탐색으로 모든 직선의 쌍을 확인하며 AD - BC = 0인 평행 상태를 걸러냄. 이후 교점 광식을 적용하고 모듈러 연산( % )으로 나머지가 0인지를 확인하여 정수 좌표만 필터링함. 도출된 정수 좌표들의 최솟값과 최댓값을 이용해 2D 문자열 배열 크기를 할당하고, 월드 좌표를 배열 인덱스로 변환하여 별( * )을 찍음.

- **Caution:**
주어지는 계수 A, B, C의 범위가 100,000이므로, AD나 BC등을 곱하는 과정에서 32비트 정수형( int )의 표현 범위를 초과할 수 있음. 반드시 교점 도출 과정의 모든 변수와 식을 64비트 정수형( long long )으로 처리하여 오버플로우를 방지해야 함.

- **Improvement:**
좌표 변환 시, 복잡한 분기문 없이 수식( maxY - y, x - minX )만으로 데카르트 좌표계를 2차원 배열 인덱스로 O(1)에 매핑하는 클린 코드를 구현함. 이를 통해 불필욯나 메모리 낭비 없이 정확히 별이 찍히는 최소 영역( Bounding Box ) 크기의 버퍼만 동적 할당함.

- **Problem Hint:**
"수평으로 뻗은 X축과 수직을 뻗은 Y축", "모든 별을 포함하는 최소한의 크기" -> 무한한 월드 공간에서 특정 객체( 별 )가 존재하는 영역만 잘라내어( Clipping ) 유한한 스크린 버퍼( 2D 배열 )로 투영시키는 렌더링 시스템의 기초 구현임을 파악.

- **Portfolio Selling Point:**
단순한 코딩 테스트 수학 문제를 넘어, 게임 엔진 내부에서 이루어지는 월드 좌표( World Coordinate ) -> 스크린 좌표( Screen Coordinate )로의 변환 원리를 완벽하게 이해하고 코드로 구현해 내는 3D 그래픽스 프로그래머로서의 기본기와 수학적 직관력을 입증.

---

## 📅 2026-04-30
**PRG 12949 - 행렬의 곱셈**
**PRG 87390 - n^2 배열 자르기**

### PRG 12949 - 행렬의 곱셈
- **Topic:** Math | Linear Algebra | Matrix Multiplication
- **Folder:** `Week_04/04_Matrix/`
- **Time Complexity:** O(M * K * N) ( M, K, N은 각각 행렬의 행과 열의 크기 )
- **Space Complexity:** O(M * N) ( 곱셈 결과가 저장되는 반환용 2차원 배열 공간 )

- **Key Idea:**
선형대수학의 행렬 곱셈 정의에 따라, 앞 행렬의 i번째 행( Row ) 벡터와 뒤 행렬의 j번째 열( Column ) 벡터를 내적( Dot Product )한 결과를 새로운 행렬의 [i][j] 위치에 누적 합산함. 이를 구현하기 위해 앞 행렬의 행 순회, 뒤 행렬의 열 순회, 그리고 내적을 위한 공통 차원 순회까지 총 3중 for문으로 구성.

- **Portfolio Selling Point:**
3D 그래픽스에서 모델( Model ), 뷰( View ), 투영( Projection ) 좌표계를 변환하는 핵심 연산인 행렬 곱셈을 라이브러리 없이 순수 알고리즘으로 구현. 2차원 공간( `vector<vector<int>>` )에 대한 인덱싱 제어력과 메모리 접근 로직을 탄탄하게 갖추었음을 입증.

---

### PRG 87390 - n^2 배열 자르기
- **Topic:** Math | Array Indexing | Memory Mapping
- **Folder:** `Week_04/04_Matrix/`
- **Time Complexity:** O(right - left) ( 최대 10^5번의 단순 수식 연산 루프)
- **Space Complexity:** O(right - left ) ( 정답을 담아 반환한 결과가 배열의 크기, 부가적인 2D 배열 없음 )

- **Key Idea:**
N x N 크기의 행렬을 1차원 배열로 펼쳤을 때, 특정 1차원 인덱스 i가 2차원 행렬에서 차지하는 위치는 row = i / n, col = i % n이 됨. 문제의 조건에 따라 각 칸에 들어가는 숫자는 해당 칸의 행과 열 중 더 큰 값에 1을 더한 max( row, col ) + 1이라는 규칙을 가짐. 따라서 배열을 직접 만들지 않고, lef부터 right까지의 1차원 인덱스만 순회하며 해당 공식을 적용해 값을 즉석에서 추출( Lazy Evaluation )할 수 있음.

- **Caution:**
N이 10^7이므로 전체 배열의 크기인 N^2은 10^14가 됨. 1차원 인덱스를 순회하는 루프 변수 i와 경계값 left, right를 32비트 정수형( int )으로 선언하면 무조건 오버플로우가 발생하여 무한 루프나 런타임 에러가 빠지므로, 반드시 64비트 정수형( long long )을 사용해야 함.

- **Portfolio Selling Point:**
알고리즘의 문제의 지문을 그대로 따라가며 물리적인 메모리( `vector<vector<int>>` )를 무식하게 할당하지 않고, 컴퓨터 아키텍처 관점에서 1차원 선형 메모리( RAM )가 2차원 데이터 구조로 매핑되는 수학적 원리를 꿰뚫어 봄. 게임 엔진에서 거대한 오픈 월드 맵이나 텍스처 데이터를 다룰 때 필수적인 '수학적 인덱싱을 통한 메모리 최적화' 역량을 확실하게 입증함.

---

## 📅 2026-05-02
**PRG 67250 - 경주로 건설**

### PRG 67250 - 경주로 건설
- **Topic:** Graph | BFS | 3D State Space | Pathfinding
- **Folder:** `Week_04/05_Pathfinding_Advanced/`
- **Time Complexity:** O(N^2) ( 각 칸마다 4방향의 최적 비용이 갱신될 때만 큐에 들어가므로, 실질적인 탐색 횟수는 4 x N^2에 비례함)
- **Space Complexity:** O(N^2) ( N x N x 4 크기의 1차원 visited 벡터 및 BFS 큐 메모리 )

- **Key Idea:**
단순한 2차원 최단 거리가 아니라 '현재 어떤 방향을 바라보며 진입했는가'에 따라 코너링 생성 여부가 달라지므로, 방문 배열을 3차원 상태( Y좌표, X좌표, 진입 방향 )로 분리하여 관리해야 함. 초기 출발지에서 오른쪽, 아래쪽으로 나아가는 두 가지 상태를 큐에 넣고 BFS를 시작하며, 이동하려는 방향이 현재 방향과 같으면 직진( 100원 ), 다르면 코너( 600원 ) 비용을 더한 뒤, 해당 상태( ny, nx, ndir )의 기존 기록보다 더 저렴할 때만 값을 갱신하고 큐에 삽입함.

- **Portfolio Selling Point:**
다차원 배열을 남용하지 않고, C++의 구조체를 이용한 깔끔한 객체지향적 상태 캡슐화와 3차원 좌표를 1차원 인덱스로 변환하는 Flatten 메서드를 직접 설계함. 이를 통해 메모리 단편화를 방지하고, 3D 클라이언트 프로그래머에게 필수적인 '다차원 메모리 선형화( Linearization )' 능력을 고난도 알고리즘 풀이에 성공적으로 녹여냄.

---

## 📅 2026-05-04
**PRG 77885 - 2개 이하로 다른 비트**
**PRG 64064 - 불량 사용자**

### PRG 77885 - 2개 이하로 다른 비트
- **Topic:** Math | Bitwise Operation | Bit Hack
- **Folder:** `Week_05/01_Bitmask/`
- **Time Complexity:** O(N) ( 배열 순회 1회, 내부 로직은 루프 없는 상수 시간 O(1))
- **Space Complexity:** O(N) ( 정답 반환용 배열 )

- **Key Idea:**
짝수는 가장 마지막 비트가 무조건 0이므로 단순히 1을 더하는 것이 정답임. 홀수는 이진수 표기 시 뒤에서부터 연속된 1이 나오다가 처음으로 등장하는 0을 찾아 1로 바꾸고, 그 직전( 바로 오른쪽 ) 비트를 0으로 바꾸는 것이 최소 증가 폭임. 이를 수학적으로 최적화하면 `가장 낮은 0비트 = ~val & ( val + 1 )` 공식을 도출할 수 있으며, 기존 값에 이 비트를 더하고 절반으로 시프트 한 값을 빼면 짝/홀 분기 없이 단번의 수식으로 정답을 구할 수 있음.

- **Caution:**
비트를 직접 시프트 연산( << )하며 찾을 경우, 피연산자 val이 최대 10^(15)이므로 C++의 32비트 기본 리터럴 연산( 1 << idx )사용 시 오버플로우가 발생하여 쓰레기값이 도출됨. 반드시 64비트 리터럴인 1LL을 사용하여 오버플로우를 예방해야 함.

- **Portfolio Selling Point:**
일반적인 시뮬레이션 방식( while문을 이용한 비트 스캔 )의 한계와 32비트 메모리 오버플로우 이슈를 파악하고, 최적화의 극의인 분기 없는 O(1) 비트 조작식을 도출해 냄. 이는 게임 클라이언트 성능 최적화에서 CPU 연산 사이클을 극한으로 아끼는 역량을 입증하는 좋은 사례임.

---

### PRG 64064 - 불량 사용자
- **Topic:** DFS | Backtracking | Bitmask
- **Folder:** `Week_05/01_Bitmask/`
- **Time Complexity:** O(N) ( N은 최대 8이므로 8! = 40,320. 매우 안전한 시간 복잡도 )
- **Space Complexity:** O(N) ( 콜 스택의 최대 깊이 8 및 정답 저장을 위한 set 메모리 )

- **Key Idea:** 
`user_id`와 `banned_id`를 하나씩 매칭하는 완전 탐색( DFS ) 문제. 이때 "어떤 유저들이 제재 목록에 들어갔는가"를 bool 배열 대신 하나의 정수( `visited` )로 관리하는 비트마스킹 기법을 사용함. 특정 유저를 포함시킬 때는 `visited | ( 1 << i )` 비트합 연산을 통해 상태를 갱신하며 재귀를 호출하고, 탐색 깊이가 banned_id의 크기와 같아지면 해당 `visited` 정수 자체를 `std::set`에 삽입하여 고유한 조합( 중복 제거 )만 남기도록 유도함.

- **Caution:**
조합의 중복을 허용하지 않으므로, 다음 깊이의 탐색을 진행하기 전 반드시 `visited & ( 1<< i )` 연산을 통해 이미 제재 목록에 포함된 유저인지 검사하는 방어 로직이 필수적임.

- **Portfolio Selling Point:**
다중 상태를 비트마스크로 제어하고, 깊이 우선 탐색의 재귀 트리를 콜 스택 오버플로우 없이 안전하게 설계함. 특히 set을 활용한 O(logN) 상태 해싱과 값 복사를 이요한 롤백 없는 백트래킹 설계는 엔진 레벨에서 대규모 컴포넌트 조합을 검사할 때 요구되는 필수 최적화 역량임.

---

## 📅 2026-05-06
**PRG 43162 - 네트워크**

### PRG 43162 - 네트워크
- **Topic:** Graph | Union-Find | Component Counting
- **Folder:** `Week_05/02_UnionFind/`
- **Time Complexity:** O(N^2) ( 인접 행렬의 절반을 순회하며, Find 연산은 경로 압축 덕분에 거의 O(1)인 아커만 함수의 역함수 시간에 수렴함 )
- **Space Complexity:** O(N) ( 크기 N의 parent 배열 )

- **Problem Hint:** 
1. 스케일 확인 : 컴퓨터의 개수 N <= 200 -> O(N^3) 알고리즘도 넉넉히 통과하는 아주 작은 데이터 크기.
2. 요구사항 분석 : A-B 연결, B-C 연결 시 A-C도 연결된 것으로 봄. -> 그래프의 간선 정보를 주면서 '연결된 덩어리'의 총 개수를 묻는 전형적인 집합 병합 문제.
3. 알고리즘 도출 : DFS/BFS 플러드 필로도 풀 수 있지만, "두 노드가 연결되어 있다"는 상태 업데이트가 반복적으로 일어나는 환경에서는 집합을 묶고 대장을 찾는 Disjoint set( 분리 집합 ) 자료구조가 가장 빠르고 논리적으로 깔끔함.

- **Portfolio Selling Point:**
STL에 의존하지 않고 Union-Find 알고리즘을 클래스 내부에서 직접 구현함. 트리 편향 현상을 막기 위한 경로 압축을 정확히 이해하고 적용하였으며, 2차원 매트릭스 탐색 시 상삼각행렬만 순회하는 최적화를 통해 연산량을 절반으로 줄이는 엔지니어적 감각을 돋보임.

---

## 📅 2026-05-07
**Segment Tree Base - 구간 합 구하기**

### Segment Tree Base - 구간 합 구하기
- **Topic:** Data Structure | Segment Tree | Divide and Conquer
- **Folder:** `Week_05/03_SegmentTree/`
- **Time Complexity:**
트리 초기화 ( Build ) : O(N)
데이터 갱신 ( Update ) : O(log N)
구간 질의 ( Query ) : O(log N)
- **Space Complexity:** O(N * 4) ( 최악의 경우 완전 이진 트리의 크기를 넉넉히 담기 위한 배열 메모리 )

- **Key Idea:**
일반적인 배열 순회 연산의 O(N)이나, 누적 합 배열이 가지는 업데이트 지연 현상 O(N)을 해결하기 위해 고안된 트리 아키텍처. 원본 배열을 반씩 분할하여 이진 트리 형태로 구간별 합계를 캐싱함. 특히 포인터( *left, *right )를 쓰지 않고 1차원 배열과 수학적 인덱싱( node * 2, node * 2 + 1 )을 통해 완전 이진 트리를 구현하여 메모리 파편화를 막음. 질의 수행 시 탐색 범위가 목표 범위 안에 완전히 포함될 경우, 하위 노드 탐색을 즉시 종료하는 분할 정복 가지치기를 통해 데이터의 변경과 조회를 모두 O(log N)에 처리하는 것이 핵심 원리임.

- **Problem Hint:**
1. 스케일 확인 : 배열의 크기 N <= 100,000, 쿼리의 수 M <= 100,000. -> O(N*M) = 100억이므로 일반 순회( for문 )은 절대 불가. 적어도 쿼리당 O(log N) 이하의 시간 복잡도가 강제됨.
2. 요구사항 분석 : "특정 인덱스의 값을 지속적으로 변경"하면서 동시에 "특정 구간의 누적 합을 조회"해야 함.
3. 알고리즘 도출:
일반 배열 : 업데이트 O(1), 구간 합 O(N) -> 실패
누적 합 배열 : 구간 합 O(1), 업데이트 O(N) -> 실패
결론 : 두 마리 토끼를 모두 O(log N)에 잡을 수 있는 세그먼트 트리가 유일한 해답이다!

- **Portfolio Selling Point:**
라이브러리에서 제공하지 않는 커스텀 자료구조인 세그먼트 트리를 클래스 형태로 캡슐화하여 완벽히 구현해 냄. 특히 구간 질의 시 발생할 수 있는 3가지 교집합 상태( 완전 배제, 완전 포함, 부분 걸침 )를 재귀 함수로 깔끔하게 분기 처리하여, 엔진 레벨의 대규모 데이터 실시간 모니터링 모듈에 즉시 투입 가능한 최적화 코드를 작성함.

---

## 📅 2026-05-12
**PRG 42627 - 디스크 컨트롤러**

### PRG 42627 - 디스크 컨트롤러
- **Topic:** Data Structure | Priority Queue | Algorithm | Greedy | SJF Scheduling
- **Folder:** `Week_06/01_PriorityQeue/`
- **Time Complexity:** O(N log N) ( 최초 배열 정렬 O(N log N) + 우선순위 큐 삽입/삭제 N번 수행 O(N log N))
- **Space Complexity:** O(N) ( 대기 큐가 최악의 경우 모든 작업을 담을 때의 메모리 )

- **Key Idea:** 
운영체제의 프로세스 스케줄링 기법 중 하나인 SJF를 구현하는 문제. 전체 작업의 평균 대기 시간을 최소화하려면, "현재 시점에서 처리가 가능한 작업들 중 가장 소요 시간이 짧은 작업을 우선적으로 처리"해야 함. 이를 위해 현재 시간 이하에 도착한 작업들을 우선순위 큐에 넣되, '소요 시간'을 기준으로 오름차순 정렬되도록 커스텀 비교 연산자를 설꼐하여 최적의 작업 순서를 도출함.

- **Caution:** 
C++의 priority_queue 커스텀 비교 연산자는 sort 함수와 반대로 동작함. 즉, `a[1] > b[1]`처럼 크다(>)를 반환해야 값이 작은 것이 큐의 최상단에 올라오는 최소 힙이 형성됨. 또한, 큐가 비어있을 때 CPU 시간을 1초씩 더하며 기다리면 시간 초과가 발생할 수 있으므로, 다음 작업의 시작 시간으로 `current_time`을 즉시 점프시키는 분기 처리가 필수적임.

- **Improvement:**
현재 코드는 `pq.emplace(jobs[job_index])`를 통해 `vector<int>` 객체 자체를 큐에 복사하여 넣고 있음. N = 500이라 성능상 문제는 전혀 없지만, 데이터 스케일이 수십만 개로 커질 경우 객체 복사 오버헤드가 발생함. 이때는 배열 전체를 복사하는 대신 jobs 배열의 인덱스만 큐에 저장하고, 비교 연산자에서 원본 jobs 배열을 차조하여 정렬하도록 개선하면 메모리와 복사 비용을 극한으로 아낄 수 있음.

- **Problem Hint:** 
1. 스케일 확인 : 작업의 수 N <= 500. -> 데이터가 매우 작아 O(N^2)도 통과하지만, 큐를 활용한 정석적인 풀이는 O(N log N)을 요구함.
2. 요구사항 분석 : 각 작업의 [대기 시간 + 처리 시간]의 평균을 가장 줄이는 방법을 찾아라. -> 먼저 들어온 순서대로 처리하면 무거운 작업 뒤에 있는 가벼운 작업들의 대기 시간이 폭증함. 따라서 가장 빨리 끝나는 녀석부터 치워야( Greedy ) 평균이 낮아짐.
3. 알고리즘 도출 :
    - 매 순간 "대기 중인 작업 중 가장 짧은 것"을 찾아야 하므로 매번 정렬하기엔 비효율적임.
    - 데이터가 지속적으로 삽입/삭제되면서 항상 최솟값을 O(log N)에 보장하는 최소 힙 기반의 우선순위 큐가 완벽한 자료구조임!

- **Portfolio Selling Point:**
C++ STL priority_queue의 기본 속성인 최대 힙을 최소 힙으로 오버라이딩하기 위해 struct cmp를 구현함. 단순히 정해진 순서를 따르는 것이 아니라, 게임 엔진 내부의 스레드 스케줄러처럼 상황에 맞춰 가장 병목이 적은 작업을 동적으로 할당하는 논리적 사고력을 입증함.

---

## 📅 2026-05-13
**PRG 42884 - 단속카메라**

### PRG 42884 - 단속카메라
- **Topic:** Algorithm | Greedy | Sorting
- **Folder:** `Week_06/02_Greedy/`
- **Time Complexity:** O(N log N) ( routes 배열을 정렬하는 데 걸리는 시간. 이후 순회는 O(N)이므로 정렬 속도가 전체 성능을 지배함. )
- **Space Complexity:** O(1) ( 정렬을 제외하고 추가적인 메모리 할당 없이 스칼라 변수 2개로 해결함. )

- **Key Idea:**
1차원 선분( 구간 )들의 교집합을 찾는 전형적인 그리디 문제. 모든 차량이 카메라를 한 번 이상 만나게 하려면, `"가장 먼저 고속도로를 빠져나가는 차량의 진출 시점"`에 카메라를 설치하는 것이 뒤따라오는 차량들을 함께 찍을 수 있는 확률이 가장 높음. 따라서 차량의 '진출 지점'을 기준으로 오름차순 정렬한 뒤, 현재 카메라 위치보다 뒤에서 진입하는 차량이 나타날 때마다 카메라를 해당 차량의 진출 지점으로 갱신( 추가 ) 하며 해답을 도출함.

- **Caution:**
커스텀 비교 함수에서 매개변수를 넘길 때 const vector<int>& a 처럼 참조자를 쓰지 않으면, 정렬 함수 내부에서 값을 교환하거나 비교할 때마다 무거운 vector 객체의 깊은 복사가 수만 번 발생하여 효율성 테스트에서 치명적인 시간 초과가 발생할 수 있음. C++에서 구조체나 컨테이너를 인자로 넘길 때는 const& 사용이 필수임.

- **Improvement:**
현재 cmp 함수를 전역/정적 함수로 분리해 두었는데, C++11 이상 모던 C++에서는 람다 표현식을 사용하여 sort 함수 내부에 인라인으로 작성하면 코드 가독성을 훨씬 높일 수 있음.
개선 예시 : sort(routes.begin(), routes.end(), [](const vector<int>& a, const vector<int>& b){ return a[1] < b[1]; } );

- **Problem Hint:**
1. 스케일 확인 : 차량의 대수 N <= 10,000. -> 데이터가 크지 않아 보이지만, 모든 차량의 겹침을 이중 루프 O(N^2)로 검사하면 약 1억 번 연산으로 효율성 테스트에서 불안정할 수 있음. 반드시 O(N log N )인 정렬 기법을 활용해야 함.
2. 요구사항 분석 : 모든 차량이 카메라를 만나도록 하는 카메라의 최소 개수를 구하라. -> 특정 구간들을 하나의 포인트로 묶어내는 문제. 국소적인 최적의 선택( 가장 빨리 나가는 차의 끝점 )이 전체의 최적해( 최소 카메라 개수 )를 보장하는 '그리디' 구조임.
3. 알고리즘 도출 :
    - 진입 지점( 시작점 ) 기준으로 정렬하면, 긴 구간 안에 짧은 구간이 포함되는 예외 케이스를 처리하기 매우 까다로워짐.
    - 따라서 무조건 진출 지점 기준으로 정렬하고, "안 찍혔으면 내 끝점에 카메라 달기"라는 단순한 논리로 루프를 돌면 끝

- **Portfolio Selling Point:**
다수의 오브젝트가 존재하는 환경에서 겹치는 구간( AABB, Bounding Box )을 판별하기 위한 Broad-Phase Collision Detection 논리를 코드로 입증함. C++의 sort와 커스텀 비교 연산자를 활용해 다중 조건을 능숙하게 다룰 수 있음을 보여줌.

---

## 📅 2026-05-14
**PRG 42898 - 등굣길**

### PRG 42898 - 등굣길
- **Topic:** Algorithm | Dynamic Programming
- **Folder:** `Week_06/03_DP/`
- **Time Complexity:** O(N x M)
- **Space Complexity:** O(N x M)

- **Key Idea:** 
오른쪽과 아래쪽으로만 이동할 수 있는 그리드 환경에서, 특정 칸 `(i, j)`에 도달할 수 있는 모든 최단 경로의 수는 "바로 위쪽 칸 `(i-1, j)`까지 오는 경우의 수" + "바로 왼쪽 칸 `(i, j-1)`까지 오는 경우의 수"의 합과 같다는 부분 문제의 중복성을 이용하는 전형적인 2차원 DP. 장애물이 존재하는 칸은 경우의 수를 강제로 0으로 초기화하여 상태 전이를 차단함.

- **Caution:**
문제 지문에서 물웅덩이 좌표가 `[m, n]` 즉, `[x( 가로 ), y( 세로 )]` 순서로 들어온다는 것을 간과하면 치명적인 런타임 에러나 오답이 발생함. 2차원 배열은 `arr[y][x]` ( 행, 열 ) 순서로 메모리에 접근하므로, 반드시 `dp[p[1]][p[0]]` 형태로 인덱스를 역전시켜 주어야 함. 또한, 값의 누적 속도가 매우 빠르므로 결과 반환 시가 아닌 루프 내부의 덧셈 연산 직후에 매번 `% MOD` 처리를 해주어야 오버플로우가 발생하지 않음.

- **Improvement:**
현재 패딩을 포함한 O(N x M) 크기의 2차원 배열을 사용 중이나, 점화식을 가만히 살펴보면 `i`번째 행을 계산할 때는 오직 `i-1`번째 행의 데이터만 필요함. 따라서 N 크기의 1차원 배열( Rolling Array 기법 ) 단 하나만 덮어씌워 가며 재사용한다면 공간 복잡도를 O(M) 수준으로 극단적으로 낮출 수 있음. ( 메모리가 극도로 제한된 임베디드나 쉐이더 환경에서 자주 쓰이는 최적화 기법 )

- **Problem Hint:**
1. 스케일 확인 : 가로 m, 가로 n은 최대 100. -> 전체 맵의 크기는 10,000. 탐색은 매우 여유로움.
2. 요구사항 분석 : 집에서 학교까지 가는 "최단 경로의 개수"를 구하라. -> 길을 '찾는' 것이라면 BFS가 적합하지만, 갈래길의 '조합의 수'를 구해야 하므로 이전 상태를 누적해 나가는 동적 계획법이 절대적으로 유리함.
3. 알고리즘 도출 :
    - 오른쪽, 아래쪽으로만 간다는 것은 "위, 왼쪽에서만 올 수 있다"는 뜻.
    - 점화식 : `DP[i][j] = DP[i-1][j] + DP[i][j-1]`
    - 예외 : 물웅덩이를 밟으면 안 되므로, 웅덩이 좌표의 DP 값은 0으로 만들어 값이 전파되지 않게 막으면 끝! 

- **Portfolio Selling Point:**
게임 엔진의 네비게이션 메시 설계 시, 특정 타일에 도달할 수 있는 다양한 경로의 가중치( Heatmap )를 계산하는 코어 로직을 입증함. 특히 `[x, y]` 순서로 주어지는 외부 데이터를 `[row, col]` 형태의 2차원 배열 인덱스로 정확히 파싱하여 공간 데이터 매핑 능력을 보여줌.

---
